{
  "name": "becca-lyria",
  "version": "13.0.0",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "test": "ng test --no-watch --browsers=ChromeHeadlessCI",
    "lint": "ng lint",
    "e2e": "ng e2e",
    "deploy": "ng deploy"
  },
  "private": true,
  "engines": {
    "node": "^16.2.0",
    "npm": "^7.13.0"
  },
  "dependencies": {
  },
  "devDependencies": {
  }
}
